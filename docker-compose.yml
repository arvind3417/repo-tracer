version: '3.9'
services:
  falkordb:
    image: falkordb/falkordb:latest
    ports:
      - "6379:6379"   # Redis-compatible port
      - "3000:3000"   # Built-in browser UI
    volumes:
      - falkordb_data:/data

volumes:
  falkordb_data:
